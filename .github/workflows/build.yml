name: Build image

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      variant:
        description: 'Repository (e.g., openwrt/openwrt, hdfla/openwrt)'
        required: true
        type: string
        default: 'hdfla/openwrt'
      branch:
        description: 'Branch or tag (e.g., main, openwrt-23.05, v23.05.4)'
        required: true
        type: string
        default: 'main'
      board:
        description: 'Board (used for config selection and cache key)'
        required: true
        type: choice
        default: 'filogic (mt798x)'
        options:
          - filogic (mt798x)
          - ramips (mt7621)
          - airoha (an75xx)
          - maple (ipq50xx)
          - cypress (ipq60xx)
          - akronite (ipq806x)
          - hawkeye (ipq807x)
          - hawkeye-nss (ipq807x-offload)
          - honeybee (qca953x)
          - scorpion (qca955x)
          - dragonfly (qcn550x)
      hostname:
        description: 'Hostname'
        required: true
        type: string
        default: 'OpenWrt'
      default_ip:
        description: 'Default IP address'
        required: true
        type: string
        default: '192.168.1.1'
      luci_theme:
        description: 'Default LuCI theme'
        required: true
        type: choice
        default: 'bootstrap'
        options:
          - bootstrap
          - aurora
          - argon
      verbosity:
        description: 'Verbose build output (V=s)'
        required: true
        type: choice
        default: 'No'
        options:
          - 'Yes'
          - 'No'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository
        uses: actions/checkout@v4

      - name: Set up build dependencies
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install -y build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
            python3-setuptools rsync swig unzip zlib1g-dev file wget
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean

      - name: Clone OpenWrt source
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.inputs.variant }}
          ref: ${{ github.event.inputs.branch }}
          path: openwrt
          token: ${{ secrets.OWRTPAT }}   # Optional, only needed for private repos
          fetch-depth: 1

      - name: Cache OpenWrt build (cachewrtbuild)
        uses: stupidloud/cachewrtbuild@main
        with:
          prefix: ${{ github.workspace }}/openwrt
          mixkey: ${{ github.event.inputs.board }}   # separate cache per board
          ccache: 'false'
          clean: 'false'

      - name: Custom feeds, packages and files
        run: |
          set -e
          cd openwrt
          # Copy feeds.conf if present
          { cp ../feeds.conf . && echo "feeds.conf copied"; } || echo "No feeds.conf"
          # Clone custom packages from packages.txt
          if [ -f ../packages.txt ]; then
            mkdir -p package
            while read url; do
              folder="${url##*/}"; folder="${folder%.git}"
              git clone "$url" "package/$folder"
              echo "Cloned $folder"
            done < ../packages.txt
          fi
          # Copy files/ overlay
          { cp -r ../files . && echo "files/ overlay copied"; } || echo "No files/ overlay"

      - name: Update feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Apply personalization
        run: |
          cd openwrt
          # Set default IP
          NEW_IP="${{ github.event.inputs.default_ip }}"
          OLD_IP_PATTERN='192.168.[0-9]\{1,3\}.1'
          sed -i "s/$OLD_IP_PATTERN/$NEW_IP/g" package/base-files/files/bin/config_generate

          # Set hostname
          HOSTNAME="${{ github.event.inputs.hostname }}"
          sed -i "s/set system.@system\[-1\].hostname='[^']*'/set system.@system[-1].hostname='$HOSTNAME'/" package/base-files/files/bin/config_generate

          # Set LuCI theme
          THEME="${{ github.event.inputs.luci_theme }}"
          sed -i "s/luci-theme-bootstrap/luci-theme-$THEME/g" feeds/luci/collections/luci/Makefile

          echo "===== Personalization: IP=$NEW_IP, Hostname=$HOSTNAME, Theme=$THEME ====="

      - name: Configure build
        run: |
          set -e
          cd openwrt
          # Generate board short name (remove parentheses and spaces)
          BOARD="${{ github.event.inputs.board }}"
          BOARD_SHORT=$(echo "$BOARD" | sed -E 's/ *\(.*\)//' | sed 's/ /-/g')
          CONFIG_FILE="../config-${BOARD_SHORT}"
          if [ ! -f "$CONFIG_FILE" ]; then
            echo "Error: Config file '$CONFIG_FILE' not found!"
            exit 1
          fi
          cp "$CONFIG_FILE" .config
          echo "Using config: $CONFIG_FILE"
          echo "Current .config (first 20 lines):"
          head -20 .config
          make defconfig
          grep -q "CONFIG_TARGET" .config || { echo "Error: .config invalid (no CONFIG_TARGET)"; exit 1; }
          echo ".config is valid."

      - name: Build firmware
        run: |
          cd openwrt
          DEBUG="${{ github.event.inputs.verbosity == 'Yes' && 'V=s' || '' }}"
          make -j$(nproc) $DEBUG
          echo "DEVICE_NAME=_${{ github.event.inputs.board }}" >> $GITHUB_ENV
          echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

      - name: Check space usage
        run: df -hT

      - name: Organize files
        id: organize
        run: |
          cd openwrt/bin/targets/*/*
          rm -rf packages
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV

      - name: Upload firmware (artifact)
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-firmware
          path: openwrt/bin/targets/

      - name: Upload firmware with timestamped name
        uses: actions/upload-artifact@v4
        with:
          name: OpenWrt_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: ${{ env.FIRMWARE }}

      - name: Generate release tag and notes
        id: tag
        run: |
          echo "release_tag=$(date +"%Y.%m.%d-%H%M")" >> $GITHUB_OUTPUT
          cat > release.txt << EOF
          ## OpenWrt Release $(date +"%Y.%m.%d-%H:%M")
          - Repository: ${{ github.event.inputs.variant }}
          - Branch: ${{ github.event.inputs.branch }}
          - Board: ${{ github.event.inputs.board }}
          - Hostname: ${{ github.event.inputs.hostname }}
          - IP: ${{ github.event.inputs.default_ip }}
          - Theme: ${{ github.event.inputs.luci_theme }}
          - Firmware files:
          EOF
          cd ${{ env.FIRMWARE }}
          for f in *.bin; do
            echo "- $f" >> release.txt
            echo "  - md5: $(md5sum "$f" | cut -d' ' -f1)" >> release.txt
          done
          # Add sha256 from sha256sums file if present
          if [ -f sha256sums ]; then
            echo "  - sha256: $(grep -oE '\w{64}' sha256sums | head -1)" >> release.txt
          fi
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Upload firmware to GitHub Release
        uses: softprops/action-gh-release@v1
        if: steps.tag.outputs.status == 'success' && !cancelled()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.release_tag }}
          body_path: release.txt
          files: |
            ${{ env.FIRMWARE }}/*.bin
            openwrt/.config
